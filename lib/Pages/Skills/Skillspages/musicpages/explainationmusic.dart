import 'package:you/Pages/Skills/Skillspages/musicpages/video_music.dart';
import 'package:flutter/material.dart';

class ExplainationMusic extends StatelessWidget {
  const ExplainationMusic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const VideoMusic(
      
    );
  }
}